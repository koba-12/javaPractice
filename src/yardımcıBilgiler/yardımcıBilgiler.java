package yardımcıBilgiler;

public class yardımcıBilgiler {

    public static void main(String[] args) {

        /*
        day08 den sonra başladım!!!


         */
        int hourlyRate = 50, // burada , koyduğum için satır sonuna alta int yazmama gerek yok
            weeklyHours = 45;

        // tam sayılarda int, ondalık sayılarda double kullanmak makbuldür
        /*
        single if = for one condition (koşul)
        if&else =for 2 condition only
        multi-branch = for 3 or more condition
        nested if = pre-condition
         */
        // == ifadesi sadece statement (ifadelerde) kullanılır. normal eşitliklerde = kullanılır
        // not: tüm statementlarda == olacak diye bir şey yok


        //  \n işareti = new line(yeni satır)
        //  \t işareti = paragraph space (paragraf boşluğu)// ve birbiri ardına gelirse araya + koyulmaz
        //genelde sout da kullanılır.


        //out hata verdiği zaman hata cümlelerinin sonundaki sayılar satır numarasıdır

        //&& = ve demek    || = veya demek

        /* charAt,lenght,indexof,substring,equals,replace,contains special metodlardır

         */
        /*
        System.out.println("I will learn \"" + topic1 + "\" and \"" + topic2 + "\" at CybertekSchool.");
        o aradaki \" işareti sayesinde topic yazan değişkenleri "" içine alır.


        int max=Integer.MAX_VALUE;
        int min=Integer.MIN_VALUE;

        System.out.println("max = " + max);//intin rangeindeki max sayıyı verir
        System.out.println("min = " + min);//intin rangeindeki min sayıyı verir.



        employees.remove(employees.size()-1);//bu sonuncu elementi verir.sanırım sadece element muhabbetlerinde -1 sonuncuyu veriyor
        tabiki lenght-1 ler için de dediğim geçerli.
        evet daha da emin oldum element muhabbetleri için geçerli olduğuna. Index 5 out of bounds for length 5-böyle bir hata veriyor.
        yani şunu diyor size ın son indexi diyelimki 5 olsun.toplamda 6 element olması gerekir.yani element index durumlarından dolayı -1 yazmalıyız


        break statement sadece loop ve switch de kullanılır.


        day32 carpet_1 classına bak += konusuyla ilgili.daha netleşecek


        day33 record da yaklaşık 50-65 dk arası bi package dışı class açmaktan bahsediyor.githuba özel class atmakla ilgili, bakılabilir.
        örneğin custom classlarda hep birbirine bağlı 2 class yaptık ya onları tek kümede toplamak için vs.
        daha önce de aynı packagede toplama için yapmıştık.bu sefer daha farklı şekilde class vs açarak anlatıyor.kolaylık olması için
        80.dk civarı discorddan task solutiona atmakla ilgili bir güzellik

        subclass,alt sınıf demektir.child class olarak da geçer.
        superclass da üst sınıftır.parant class olarak da geçer.


        day38 yaklaşuk 20-23 arası github ve commitle ilgili bir şetler anlatıyor bakılabilir.



        exit code 1 = bir şeyler yanlış gitti.
                 */
    }
}
